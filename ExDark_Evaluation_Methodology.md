# Retinex-TDN 目标检测下游任务评估方案 (基于 ExDark)

## 1. 评估目的
本实验旨在通过**下游目标检测任务（Downstream Object Detection Task）**，客观、量化地评估 `Retinex-TDN` 模型在低光图像上的 **Retinex 分解质量**（特别是反射分量 $R$ 的保真度）。
相比于易受主观偏好影响的视觉对比，机器视觉网络对图像的高频语义、边缘结构和噪声更为敏感。在检测任务上的优异表现，能强有力地证明分解算法成功去除了光照退化，且未引入破坏语义的伪影。

## 2. 核心实验逻辑
**核心原则：使用在正常光照数据集（COCO）上预训练的冻结网络进行直接测试，绝对不可在低光数据集上重新训练或微调。**

*   **把检测器作为客观的“裁判”：** COCO 预训练的 YOLO 网络习惯于处理正常光照下的清晰图像。
*   **消除域偏移（Domain Shift）：** 原始低光图像输入检测器必然导致极低的 mAP。低光分解任务的本质是将图像从“低光域”映射回“正常光域”。
*   **证明分解的纯粹性：** 如果将算法提取出的反射分量 $R$ 输入该冻结网络，网络能够准确检测出目标，则从数学和语义上证明了该 $R$ 分量成功剥离了光照影响，完美保留了物体的固有属性（Illumination-invariant），且未放大破坏边缘的噪声。

## 3. 实验对象与变量控制
*   **测试数据集：** ExDark (Exclusively Dark Image Dataset) 的 Test 集或 Val 集。
*   **评测网络：** YOLOv8 官方 COCO 预训练模型（如 `yolov8s.pt` 或 `yolov8m.pt`），保持权重完全冻结。
*   **对比组设置：**
    *   **Baseline:** 原始低光图像。
    *   **Competitors:** 经典及最新的 Retinex/低光增强模型（如 RetinexNet, KinD, SCI, RUAS 等）提取的 $R$ 分量（或增强图）。
    *   **Ours:** `Retinex-TDN` 提取的 $R$ 分量。

## 4. 实施步骤指南

### 阶段一：图像准备与推理（Inference）
1.  整理 ExDark 测试集图像及其对应的 Ground Truth (GT) 标注文件。
2.  使用 `Retinex-TDN` 的测试脚本，遍历 ExDark 测试集，提取分解出的反射分量 $R$。
3.  将张量范围映射回 `[0, 255]`，并将其保存为标准的 RGB 图像格式（如 `.png`）。
4.  对其他对比方法重复上述步骤。

### 阶段二：YOLO 目标检测
1.  加载预训练好的 `yolov8s.pt` 模型。
2.  分别将 Baseline、竞品生成的图像、Ours 生成的图像目录作为输入，执行推理预测。
3.  获取并保存每张图像的预测框（Bounding Boxes）、类别 ID 及置信度。

### 阶段三：类别过滤与评估（Evaluation）
*   **类别对应（关键点）：** ExDark 数据集包含 12 个类别（Bicycle, Boat, Bottle, Bus, Car, Cat, Chair, Cup, Dog, Motorbike, People, Table）。这 12 个类别完全包含于 COCO 的 80 个类别中。
*   **过滤操作：** 编写评估脚本，遍历 YOLO 的预测结果，**直接丢弃所有不属于上述 12 个类别的预测框**。同时，将 COCO 中的类别名称（如 "person", "dining table"）映射为 ExDark 的标签规范。
*   **计算 mAP：** 使用 COCO API 或标准的 mAP 计算脚本，对比过滤后的预测结果与 ExDark 的官方 GT，计算出最终的 mAP 分数。

## 5. 预期结果与论文话术
实验将汇总为一张对比表格。预期的核心结论如下：

> "由于目标检测器仅在正常光照域（COCO）训练，其在低光图像（Baseline）上的 mAP 表现极差。传统的对比方法在分解时往往在 $R$ 分量中残留噪声或造成严重的纹理模糊，破坏了机器视觉所需的语义特征，导致 mAP 提升有限。相比之下，`Retinex-TDN` 展现出了最卓越的光照-反射率解耦能力。其提取的 $R$ 分量不仅视觉上纯净，更完美保留了高频边缘结构，使得预训练的检测网络能够无缝衔接。在未进行任何微调的情况下，基于我们分解结果的检测 mAP 达到了最高，这客观且充分地证明了我们算法在底层视觉恢复和高层语义保真度上的优越性。"
