# Retinex-TDN 自动驾驶目标检测评估方案 (基于 BDD100k)

## 1. 评估目的
本实验旨在复杂的自动驾驶真实场景（行车记录仪视角）下，评估 `Retinex-TDN` 的低光分解质量。
与 ExDark 不同，BDD100k 包含大量远距离小目标（如车辆、行人）和特定类别（如红绿灯）。通过严格控制变量的实验设计，可以完美隔离**“场景域偏移（视角/类别差异）”**，纯粹地量化模型解决**“光照域偏移（昼夜差异）”**的能力。

## 2. 为什么不复用 COCO 预训练模型？
*   **双重域偏移干扰：** COCO 主要是日常视角的大目标图像。如果直接用 COCO 检测器测试 BDDnight，检测器不仅会因为“光线暗”而失败，还会因为“没见过行车记录仪视角”和“目标太小”而失败。
*   **结论不纯粹：** 这种情况下 mAP 的降低无法准确归因于是“检测器不行”还是“你的低光增强算法不行”。

## 3. 核心实验方案："Train on Day, Test on Night"
此方案是目前自动驾驶低光视觉领域最严谨、最能证明底层图像恢复质量的实验范式。

### 阶段一：培养“全天候白天驾驶员”（训练）
*   **操作：** 使用 YOLO（如 YOLOv8）在 **BDDday (白天子集)** 的 Training Set 上从头训练（或微调）一个检测器，记为 `YOLO-Day`。
*   **目的：** 让网络完美掌握自动驾驶视角的特征、物体大小和类别分布，但确保它**对黑夜一无所知**。

### 阶段二：确立性能天花板与地板（基线测试）
在此阶段及后续测试中，`YOLO-Day` 的权重必须**完全冻结**。
*   **天花板 (Oracle Bound)：** 使用 `YOLO-Day` 测试 `BDDday` 的 Test/Val 集。此时没有光照干扰，得到的 mAP 为最高理论值（如 50%）。
*   **地板 (Lower Bound)：** 使用 `YOLO-Day` 测试原始的 `BDDnight` Test/Val 集。由于网络没见过黑夜，性能必然暴跌，得到的 mAP 为最低基准（如 15%）。

### 阶段三：光照域恢复测试（你的核心舞台）
*   **操作：** 使用你的 `Retinex-TDN` 及各竞品模型，处理 `BDDnight` 的测试集，提取出反射分量 $R$（或最终增强图），保存为标准 RGB 图像。
*   **测试：** 将这些不同模型生成的夜间 $R$ 图像，输入冻结的 `YOLO-Day` 进行预测，计算 mAP。
*   **评价标准：** 谁的 mAP 越接近天花板（Day test Day 的成绩），说明谁最大程度地抹平了昼夜差异，分解出的 $R$ 最贴近白天的固有反射率。

## 4. 附加应用实验："Data Augmentation 测试" (基于 BDDboth)
如果你想在论文中单独增加一节证明模型的“工程落地价值”，可以使用包含白天和黑夜的 `BDDboth` 数据集做对比：
1.  **Baseline 训练：** 直接使用原始的 `BDDboth` 训练集训练一个通用的 YOLO 网络，并测试。
2.  **Ours 增强训练：** 将 `BDDboth` 训练集中的**所有夜间图像**，替换为你用 `Retinex-TDN` 增强清洗后的 $R$ 图像。拿这个“清洗过的混合数据集”去训练 YOLO 并测试。
*   **目的：** 证明你的模型可以作为极佳的上游预处理器（ISP Pipeline）。经过你的算法清洗数据后，能训练出泛化能力更强、精度更高的通用自动驾驶视觉系统。

## 5. 预期结果与论文话术
实验图表应展示 Oracle, Baseline, Competitors, Ours 的 mAP 对比。预期的核心结论如下：

> "为了严格隔离行车记录仪视角的域偏移干扰，纯粹评估算法的光照恢复能力，我们采用了 'Train on Day, Test on Night' 的实验范式。由于光照域偏移，在白天图像上训练的检测器在面临极暗的夜间原始图像（Baseline）时，性能发生严重衰退。
> 通过对比发现，基于我们的 `Retinex-TDN` 模型提取的夜间图像反射分量 $R$，最大程度地抹平了昼夜之间的光照差异。其恢复的高频语义和结构特征，使得原本只适应白天场景的检测器能够无缝工作，实现了各项对比方法中最高的 mAP 恢复率。这证明了我们的分解算法在复杂自动驾驶场景下不仅视觉上高度保真，更具有坚实的机器视觉应用价值。"
