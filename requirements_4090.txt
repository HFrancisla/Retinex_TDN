# Retinex-TDN 依赖 (RTX 4090 实测可跑)
# Python 3.12 | PyTorch 2.12 + CUDA 13.0 (cu130)
#
# 安装前请先确认 CUDA 驱动 ≥ 535.54 (即支持 CUDA 13.0)
# 安装命令:
#   pip install -r requirements_4090.txt --index-url https://download.pytorch.org/whl/cu130
#
# 如果 pip 自动解析的 torch 不带 CUDA，可单独指定:
#   pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cu130
#   pip install -r requirements_4090.txt

torch==2.12.0
torchvision==0.27.0
einops==0.8.2
pywavelets==1.9.0
pyyaml==6.0.3
numpy==2.4.6
opencv-python==4.13.0.92
Pillow==12.2.0
scikit-image==0.26.0
lpips==0.1.4
thop==0.1.1-2209072238
tqdm==4.68.2
matplotlib==3.11.0
tensorboard==2.20.0
